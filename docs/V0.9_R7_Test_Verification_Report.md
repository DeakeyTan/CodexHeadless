# v0.9 R7 Test Verification Report

Version: `0.9.0-uat.1`
Date: 2026-07-11
Environment: macOS 27.0, Xcode 27.0 beta (`27A5218g`), Swift 6.4, Apple Silicon host.

## Result

| Requirement | Status | Evidence |
|---|---|---|
| R7-P0-01 XCTest activation | DONE | Full Xcode XCTest executed successfully. |
| R7-P0-02 logger process boundary | DONE | Separate `CodexHeadlessTestHelper` process replaces unavailable `fork()`; suite executed. |
| R7-P0-03 model type drift | DONE | Fixtures use current `String?` process-start identity; suite compiles and executes. |
| R7-P0-04 exact display binding | DONE | Exact reported ID, managed identity, host identity, stale-ID and unrelated-display tests passed. |
| R7-P1-02 exact helper detection | DONE | Token-level discovery, identity verification, false-positive and true-positive tests passed. |
| R7-P1-04 dialog suppression | DONE | Restore/timer race and suppression lifecycle tests passed. |
| R7-P1-05 R6 failure paths | DONE | Capability, launch protocol, cleanup and recovery tests executed in the full suite. |
| R7-P1-06 repeated quality gate | DONE | Three consecutive final runs: 145 tests, zero failures each. |

## Actual XCTest Runs

The repository is stored in iCloud Drive. A default `.build` test invocation inherited Finder metadata and failed during test-bundle code signing, so tests were executed from an isolated local scratch directory. This changes only build location, not sources or test selection.

Command used for each run:

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
swift test --build-system native \
  --scratch-path /private/tmp/CodexHeadless-R7-tests
```

| Run | Exit code | Executed | Failures | XCTest duration |
|---|---:|---:|---:|---:|
| 1 | 0 | 145 | 0 | 9.360s |
| 2 | 0 | 145 | 0 | 10.079s |
| 3 | 0 | 145 | 0 | 9.695s |

The Swift Testing runner additionally reported zero tests; the 145 recorded executions above are XCTest cases and are the claimed test count.

## Other Verification

| Command | Exit code | Status |
|---|---:|---|
| `swift build --build-system native` | 0 | DONE |
| `swift build --build-system native --arch arm64` | 0 | DONE |
| `swift build --build-system native --arch x86_64` | 0 | DONE |
| `bash -n scripts/*.sh` | 0 | DONE |
| Ruby YAML load for `.github/workflows/*.{yml,yaml}` | 0 | DONE |
| `git diff --check` | 0 | DONE |
| `bash scripts/audit_critical_reads.sh` | 0 | DONE |
| `bash scripts/audit_app_responsiveness.sh` | 0 | DONE |
| `bash scripts/audit_r6_helper_protocol.sh` | 0 | DONE |
| `bash scripts/audit_r7_uat_safety.sh` | 0 | DONE |
| `.build/debug/codex-headless self-test` | 0 | DONE |

## CI

No commit or push was requested, so no GitHub Actions run URL or run ID exists. CI execution is `NOT VERIFIED`; local full-Xcode execution is `DONE`.
