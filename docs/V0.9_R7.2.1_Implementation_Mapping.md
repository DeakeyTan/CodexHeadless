# v0.9 R7.2.1 Implementation Mapping

Status: Implemented and verified (`DONE`)
Development version: `0.9.0-dev`
Active PRD: `CodexHeadless_PRD_V0.9_R7.2.1_Final_Safety_Closure_and_Logging_Control.md`

> No broad audit expansion is authorized.

This document records the approved minimal implementation. Estimates are retained beside measured R7.2.1 diff values from the pre-implementation baseline.

## Constraint Decision

`CONSTRAINT BLOCKED` is not triggered. Every requirement can reuse an existing production path without a new coordinator, cache, recurring scheduler, state machine, background process, or persistent workflow-state field.

The replacement-loss confirmation will use one bounded `DispatchQueue.asyncAfter` work item on an existing background queue. It is not a recurring timer and does not change the existing Headless reconciliation cadence.

## Requirement Plan

### R7.2.1-P1-01 — Replacement Display Continuity

| Planning item | Decision |
|---|---|
| Existing code reused | `PeriodicMaintenancePolicy` continues to request `.reconcileManagedResources`; `StatusBarInteractionCoordinator.schedulePeriodicMaintenance()` receives the resulting `OperationalEvidence`; `ControllerOperationCoordinator` supplies the existing maintenance and user-operation queues; automatic recovery calls the existing `runRestoreControllerOperation(...)` and `HeadlessController.restoreNormal()` path. RuntimeState's existing `replacementDisplayID`, `replacementDisplayType`, `replacementDisplayReady`, `replacementDisplayPromoted`, `virtualDisplayID`, `virtualDisplayCreated`, mode, and Journal-derived operation ID remain authoritative. |
| Planned production files | `OperationalEvidence.swift`, `OperationalEvidenceAssessor.swift`, `StatusBarController.swift`, `StatusBarInteractionCoordinator.swift` |
| New production files | No |
| New production types | One small value type, `ReplacementLossSuspect`, stored in an existing file. It binds expected mode, operation ID, and replacement display ID for one bounded confirmation. It is not a state machine or coordinator. |
| Estimated production diff | +125 / -15 lines |
| Final implementation diff | Incremental P1-01 diff is +77 / -1 lines in the four planned files; one new production type (`ReplacementLossSuspect`); no new production file, timer, queue, coordinator, cache, or Restore path. |
| Existing behavior changed | Confirm Required and Headless periodically verify the exact replacement ID. One unusable observation records a suspect only. A matching second observation after approximately 1–2 seconds submits the existing Restore operation once. Reappearance, operation change, mode change, stale evidence, Journal/process observation failure, or temporarily unknown ownership cancels/refuses automatic Restore. User-visible Restore state remains available when persistent logging is off. |
| Planned tests | Extend `OperationalEvidenceAssessorTests` for healthy physical/managed replacement, exact-ID selection with multiple displays, inactive/offline physical display, managed wrong ID, and ownership-unknown behavior. Add focused `ReplacementContinuityTests` using the existing evidence fixtures/clock style for first miss, recovery, matching second miss, mode/operation cancellation, and duplicate suppression. Existing Restore presentation/workflow tests prove the submitted operation uses the normal Restore path. |
| Explicitly deferred | No monitor daemon; no second scheduler; no permanent high-frequency polling; no direct built-in enable or virtual-host termination from the observer; no auto-Restore for stale/unreadable evidence or ownership unknown; no generalized display-health rules engine. |

Implementation status: `DONE` — focused continuity/Restore tests and final full gate passed; the targeted audit verifies submission through the existing Restore wrapper.

### R7.2.1-P1-02 — Limited Operational Evidence Consistency Closure

| Planning item | Decision |
|---|---|
| Existing code reused | Extend `OperationalDisplayEvidence`, `OperationalEvidenceViolation`, and the current single-pass `OperationalEvidenceAssessor.assess(...)`. Reuse the already supplied process snapshot and display enumeration. Keep the existing built-in soft-disconnect direction check. |
| Planned production files | `OperationalEvidence.swift`, `OperationalEvidenceAssessor.swift` |
| New production files | No |
| New production types | No; add fields/cases to existing evidence value types only. |
| Estimated production diff | +35 / -15 lines |
| Final implementation diff | +52 / -5 lines in the two planned evidence files, including the replacement evidence fields shared with P1-01; no new production type or file. |
| Existing behavior changed | Confirm Required/Headless additionally require `state.keepAwake == true`; managed display lookup uses exact `virtualDisplayID` instead of the first managed display; replacement continuity records exact expected ID, enumeration, active/online/main/managed facts. Unrelated managed or external displays no longer substitute for the expected display or create a false ID mismatch. |
| Planned tests | Extend `OperationalEvidenceAssessorTests` for `state.keepAwake=false`, exact expected managed display when another appears first, absent expected managed display, exact physical replacement, unrelated external display, and unchanged built-in soft-disconnect behavior. Reuse `WorkflowHarness`, fake managers, supplied snapshots, and `makeDisplay`. |
| Explicitly deferred | Complete bidirectional RuntimeState/Journal matrix; automatic cleanup of unexpected helper processes or unrelated managed displays; exhaustive Fallback/Error/Preparing combinations; non-critical reverse mismatches; test-root attack expansion. |

Implementation status: `DONE` — exact-ID, Keep Awake and built-in consistency focused tests and final full gate passed.

### R7.2.1-P1-03 — Operational Cache and Transition Lifecycle

| Planning item | Decision |
|---|---|
| Existing code reused | Extend `OperationalEvidenceCache` compatibility checks, `StatusBarController.completeOperationalEvidence`, `OperationalTransitionDiagnostic`, and current operation completion/periodic rollback handling in `StatusBarInteractionCoordinator`. Reuse `cleanNormalCache`, `operationalEvidenceCache`, menu refresh, and existing completion callbacks. |
| Planned production files | `OperationalEvidenceCache.swift`, `OperationalTransitionDiagnostic.swift`, `StatusBarController.swift`, `StatusBarInteractionCoordinator.swift` |
| New production files | No |
| New production types | No |
| Estimated production diff | +75 / -30 lines |
| Final implementation diff | Incremental P1-03 diff is +45 / -9 lines in `OperationalEvidenceCache.swift`, `StatusBarController.swift`, and `StatusBarInteractionCoordinator.swift`. `OperationalTransitionDiagnostic.swift` required no production change because it already treated operation identity as material; focused tests were expanded instead. No new type or file. |
| Existing behavior changed | Cached evidence is usable only for a compatible RuntimeState mode and expected operation identity. A new Enable retires old evidence before intentional non-Normal transition. One shared verified-Normal helper clears Operational Evidence and refreshes Clean Normal for user Restore, automatic rollback, Enable compensation, launch Normal, and resumed Restore completion. Previous transition presentation is constructed from the previous evidence's own mode and facts. Materially unchanged cycles remain deduplicated; operation changes are not deduplicated as the same session. |
| Planned tests | Extend `OperationalEvidenceCacheTests` for mode mismatch, operation mismatch, refresh history, and new-workflow isolation. Extend `OperationalTransitionDiagnosticTests` for Confirm Required→Headless, Headless→Restoring, previous-mode interpretation, operation change, and unchanged-cycle deduplication. Extend `PeriodicMaintenancePolicyTests`/existing App responsiveness source audit for rollback cleanup without adding a timer. |
| Explicitly deferred | Persistent operational-cache state; a second cache; generalized transition engine; broader menu performance work; complete transition matrices for deferred modes. |

Implementation status: `DONE` — cache/transition/rollback focused tests and final full gate passed.

### R7.2.1-P1-04 — Fail-Closed LaunchAgent Verification

| Planning item | Decision |
|---|---|
| Existing code reused | Keep `UninstallSessionCoordinator`, its workflow-lock lifetime, deletion ordering, `SystemUninstallEntryPointManager`, `ShellResult`, and current exact App executable verification. Replace only the ambiguous `launchctl print` interpretation. |
| Planned production files | `UninstallSessionCoordinator.swift` |
| New production files | No |
| New production types | One small enum, `LaunchAgentObservation` (`loaded`, `notLoaded`, `unverified(reason)`), in the existing uninstall file. Command execution is injected as a closure or interpreted by a pure function; no shell framework/protocol hierarchy is added. |
| Estimated production diff | +65 / -15 lines |
| Final implementation diff | +40 / -2 lines in the one planned production file; one new production enum (`LaunchAgentObservation`); no new file or command framework. |
| Existing behavior changed | Successful target-service observation remains loaded. Only an explicitly recognized service-not-found result becomes not loaded. Timeout, signal, launch error, domain failure, empty/unknown output, and unexpected exit are unverified and refuse uninstall before deletion, preserving App and CLI. Existing atomic lock coverage is unchanged. |
| Planned tests | Extend `UninstallSessionCoordinatorTests` with loaded, recognized not-loaded, timeout, signal, thrown launch error, unknown non-zero result, unverified-before-deletion, App/CLI preservation, and exact-App verification regression cases. Reuse existing tracking entry points/deleter/lease and `ShellResult`; no new test framework. |
| Explicitly deferred | Generalized command execution framework; broader LaunchAgent management redesign; changes to uninstall target paths, deletion order, temporary coordinator, or workflow lock. |

Implementation status: `DONE` — focused XCTest, both uninstall shell tests, atomic lock regression, and final full gate passed.

### R7.2.1-P1-05 — Persistent Diagnostic Logging Switch

| Planning item | Decision |
|---|---|
| Existing code reused | Add one optional flat AppConfig field equivalent to `logging.enabled`; reuse `ConfigManager` load/save/mutation and config health behavior; reuse `CHLogger`; reuse the existing Diagnostics submenu and `runSettingsMutation`; reuse CLI `config get/set`; reuse inherited process environment for all existing helper launch paths. Status output continues through `StatusReportBuilder`. |
| Planned production files | `Models.swift`, `ConfigManager.swift`, `Logger.swift`, `StatusReportBuilder.swift`, `StatusBarController.swift`, `CLIExecutor.swift` |
| New production files | No |
| New production types | One small thread-safe `DiagnosticLoggingPolicy` class in `Logger.swift`. No logger manager, levels, categories, service container, or new cache. |
| Estimated production diff | +170 / -25 lines |
| Final implementation diff | +99 / -4 lines across the six planned production files; one new production type (`DiagnosticLoggingPolicy`); no new production file. |
| Existing behavior changed | Effective diagnostic logging defaults to false for new, old, missing, or invalid settings. App and ordinary CLI load config once before their first persistent diagnostic line and update the shared in-memory policy. Disabled `CHLogger` returns before directory creation, lock/open/flock, rotation, formatting, write, and fsync. Enabling/disabling takes effect immediately and updates inherited `CODEX_HEADLESS_DIAGNOSTIC_LOGGING`, so existing `Process`/`Shell` helper launches need no parallel launch path. Helper protocol stdout/stderr, CLI output, App alerts, exit codes, safety decisions, and historical log files remain unchanged. Diagnostics menu exposes the toggle; CLI accepts `config get/set logging.enabled`; status/config output reports Enabled/Disabled. Safe-default profile application preserves the explicit logging preference except `config reset defaults`, which intentionally returns it to false. |
| Planned tests | Add `LoggerConfigurationTests` for defaults, legacy/missing/invalid config, no log/lock/rotation/write while disabled, immediate enable/disable, historical files, and unchanged returned errors/presentation. Update `LoggerProcessBoundaryTests` and `CodexHeadlessTestHelper` to explicitly enable logging for cross-thread/process integrity tests. Extend `ConfigManagerTests`, `ManagedProcessSnapshotTests`, `InternalHelperAuthorizationTests`, CLI config/exit-code tests, and status report tests for propagation and user-visible behavior. Add an App bootstrap structural audit proving default startup creates no diagnostic file. |
| Explicitly deferred | Logging levels/categories; telemetry/upload; log viewer/export; automatic deletion or “Clear Logs”; suppression of user-facing output; crash-report control; broad logger API conversion to lazy/autoclosure messages. |

Implementation status: `DONE` — default/migration, no-file/no-lock/no-rotation, immediate disable, cross-process and process-snapshot focused tests plus final full gate passed.

## Planned Production File Set

The following 12 existing production files are the complete planned set:

1. `Sources/CodexHeadlessCore/Models.swift`
2. `Sources/CodexHeadlessCore/ConfigManager.swift`
3. `Sources/CodexHeadlessCore/Logger.swift`
4. `Sources/CodexHeadlessCore/OperationalEvidence.swift`
5. `Sources/CodexHeadlessCore/OperationalEvidenceAssessor.swift`
6. `Sources/CodexHeadlessCore/OperationalEvidenceCache.swift`
7. `Sources/CodexHeadlessCore/OperationalTransitionDiagnostic.swift`
8. `Sources/CodexHeadlessCore/UninstallSessionCoordinator.swift`
9. `Sources/CodexHeadlessCore/StatusReportBuilder.swift`
10. `Sources/CodexHeadlessApp/StatusBarController.swift`
11. `Sources/CodexHeadlessApp/StatusBarInteractionCoordinator.swift`
12. `Sources/CodexHeadlessCLI/CLIExecutor.swift`

No change is planned for `PeriodicMaintenancePolicy`, `ControllerOperationCoordinator`, `RuntimeWorkflowCoordinator`, helper managers, private bridges, `Shell`, or `RuntimeState`. Their existing behavior/path is reused.

The required focused audit script, test files, and R7.2.1 reports are not production source and are excluded from the production-line budget.

## Complexity Budget

| Metric | Planned value | Limit / target | Result |
|---|---:|---:|---|
| Production files changed | 11 | target ≤ 12 | PASS |
| Production files added | 0 | ≤ 3 | PASS |
| Production lines added | 313 | changed-line target considered below | PASS |
| Production lines removed | 21 | changed-line target considered below | PASS |
| Total production lines changed | 334 | target ≤ 1,500 | PASS |
| New production types | 3 | ≤ 5 | PASS |
| New recurring timers | 0 | 0 | PASS |
| New caches | 0 | 0 | PASS |
| New coordinators | 0 | 0 | PASS |
| New persistent workflow-state fields | 0 | 0 | PASS |
| New state machines | 0 | 0 | PASS |
| Unrelated refactors | 0 | 0 | PASS |

The one planned optional persistent configuration field controls logging; it is AppConfig, not RuntimeState, Recovery Journal, cleanup progress, or another persistent workflow-state field.

## Constraint Confirmation

- New production files no more than 3: **YES** (`0`).
- New production types no more than 5: **YES** (`3`).
- Production code change target no more than 1,500 lines: **YES** (`334` additions plus removals).
- New recurring timer = 0: **YES**.
- New cache = 0: **YES**.
- New coordinator = 0: **YES**.
- New state machine = 0: **YES**.
- New persistent workflow-state field = 0: **YES**.

## Verification Plan After Approval

The commands below were executed after implementation; exact results are recorded in the Test Verification Report:

```text
swift build
swift build --arch arm64
swift build --arch x86_64
swift test --filter LoggerConfigurationTests
swift test --filter LoggerProcessBoundaryTests
swift test --filter OperationalEvidenceAssessorTests
swift test --filter OperationalEvidenceCacheTests
swift test --filter OperationalTransitionDiagnosticTests
swift test --filter UninstallSessionCoordinatorTests
bash scripts/run_xctest_gate.sh
bash scripts/audit_critical_reads.sh
bash scripts/audit_app_responsiveness.sh
bash scripts/audit_r6_helper_protocol.sh
bash scripts/audit_r7_uat_safety.sh
bash scripts/audit_r7_1_code_gate.sh
bash scripts/audit_r7_2_code_gate.sh
bash scripts/audit_r7_2_1_final_patch.sh
git diff --check
```

At least one final clean XCTest run is required; three consecutive clean runs are preferred. Reports must keep the Patch Gate separate from the product-owner UAT Gate.

## Implementation Stop

All explicit R7.2.1 requirements are `DONE`. Feature and architecture development stops here pending independent audit and product-owner UAT.
