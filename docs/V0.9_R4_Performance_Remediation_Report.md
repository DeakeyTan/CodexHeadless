# V0.9 R4 Performance Remediation Report

## Scope

- Active PRD: `CodexHeadless_PRD_V0.9_R4_Performance_Responsiveness_and_R3_Closure.md`
- Independent-audit follow-up: `CodexHeadless_PRD_V0.9_R5_Configuration_Mutation_Observation_Integrity.md`
- Version: `0.9.0-dev`
- Base commit: `a5c21ac38bf6e61a95063566e4c8a2a1d3200340`, dirty workspace
- Architecture under local measurement: Apple Silicon `arm64`, macOS `27.0`

## Issue Status

| Issue | Status | Implementation | Tests / remaining evidence |
|---|---|---|---|
| R4-P0-01 | PARTIALLY DONE | `SleepManager` and `VirtualDisplayManager` now compensate RuntimeState before clearing Journal ownership after a post-commit cleanup. Compensation failure retains owner and cleanupPending. Normal cleanup follows the same ordering. | Manager compensation tests added; XCTest executed 0. |
| R4-P0-02 | PARTIALLY DONE | Every `.alreadyNormal` and `.completed` exit routes through `evaluateRestoreSuccess`. It permits only the expected finalizing Journal, performs one shared-snapshot authoritative gate, finalizes/deletes Journal, then performs a lightweight post-delete check. Cooldown is calculated immediately before Normal persistence. | `RestoreSuccessGateTests`; XCTest executed 0. |
| R4-P1-01 | PARTIALLY DONE | `Shell.run` drains stdout/stderr concurrently, reports `timedOut` and monotonic `durationMilliseconds`, performs TERM/KILL, and retains output. | Self-test large output completed without deadlock; XCTest suite did not execute. |
| R4-P1-02 | PARTIALLY DONE | Menu and settings presentation use `CleanNormalAssessmentCache`; authoritative assessment runs on a utility queue or in Core after workflow lock. Stale cache displays `Safety: Checking...`. | App source boundary audit exit 0; target UI timing not measured. |
| R4-P1-03 | PARTIALLY DONE | User operation is reserved and queued before asynchronous UI refresh. User and maintenance queues are separate; pending maintenance is discarded when superseded. Refreshes coalesce. Immediate title acknowledgment precedes disk/config preflight. | Source tests added; target request/start and UI callback targets not measured. |
| R4-P1-04 | PARTIALLY DONE | Pure mode-aware policy: Normal cache audit 30s, Headless reconcile 10s, confirmation rollback 1s, RestorePaused availability 1s. No-op leases disable lifecycle logging. | Policy tests added; one-hour log growth not measured. |
| R4-P1-05 | PARTIALLY DONE | One `ManagedProcessSnapshot` is shared by Keep Awake and Virtual observations. Snapshot failure maps to unknown. Restore cleanup uses targeted known-PID verification and one final shared full snapshot. | Snapshot tests added; XCTest executed 0. |
| R4-P1-06 | PARTIALLY DONE | Touch Bar restore failure returns cleanupIncomplete and preserves state/Journal. Journal now records whether CodexHeadless changed Touch Bar state; Safe Restore no longer assumes hidden. Enable compensation also blocks on failed restore. | Retry tests added; Intel Touch Bar not tested. |
| R4-P1-07 | PARTIALLY DONE | Standalone Journal is deleted only when both resources and RuntimeState are clean and a successful process snapshot finds no helper. Failure preserves Journal. | Lifecycle tests added; XCTest executed 0. |
| R4-P2-01 | PARTIALLY DONE | Capability schema v2 binds helper kind to exact active Journal operation/stage, parent, executable identity, expiry and nonce. Consumed/expired current-schema files are removed; future schema is preserved. | Stage tests added; XCTest executed 0. |
| R4-P2-02 | PARTIALLY DONE | Monotonic `[Perf]` spans cover request/submission, queue/Core start, Clean Normal, process snapshot, Keep Awake, virtual display, Enable/Restore Core, takeover, final verification, Core/UI and slow menu rebuild. | Instrumentation build verified; hardware metrics not collected. |
| R4-P2-03 | PARTIALLY DONE | File logging is serialized, keeps one handle, flushes errors/completion records, rotates at 5 MiB and retains three files. No-op maintenance logs were removed. | Concurrency and one-hour growth require executed tests/manual run. |
| R4-P2-04 | BLOCKED | CI configuration contains full-Xcode test and log artifacts, but current uncommitted workspace cannot be run remotely without commit/push, which were prohibited. Local CLT lacks XCTest. | Actual XCTest count 0; no CI URL/run ID exists. |
| R5-P1-01 | PARTIALLY DONE | Configuration mutation now requires a successful shared process snapshot; timeout/failure is unknown and refuses mutation. Critical-read audit includes the guard. | Dedicated tests added; XCTest executed 0. |

## Main Modified Files

- Shell/snapshot: `Shell.swift`, `ManagedProcessSnapshot.swift`, `ManagerProtocols.swift`, `CleanNormalAssessment.swift`.
- App: `StatusBarController.swift`, `StatusBarInteractionCoordinator.swift`, `StatusBarSettingsActions.swift`, `StatusBarDiagnosticsActions.swift`, `ControllerOperationCoordinator.swift`.
- Resource truth: `SleepManager.swift`, `VirtualDisplayManager.swift`, `StandaloneJournalFinalizer.swift`, `RestoreWorkflow.swift`, `RecoveryCoordinator.swift`, `Models.swift`.
- Authorization/config: `HelperCapability.swift`, `ConfigurationMutationGuard.swift`.
- Efficiency/docs: `Logger.swift`, `SelfTest.swift`, `README.md`, `docs/RECOVERY_GUIDE.md`, CI and audit scripts.

## Before / After Evidence

| Metric | Before, supplied R4 log | After local evidence | Status |
|---|---:|---:|---|
| Enable request to accepted | about 12s | Instrumented, target hardware run required | NOT VERIFIED |
| Core Enable | about 4s after first meaningful phase | Instrumented, target hardware run required | NOT VERIFIED |
| Core Restore | about 11s | Instrumented, target hardware run required | NOT VERIFIED |
| Core finish to Restore UI | about 4s | Callback is dispatched immediately and instrumented | NOT VERIFIED |
| Full snapshots per assessment | previously up to 2 | source path captures 1 and shares it | PARTIALLY DONE |
| Full snapshots in final Restore phase | previously up to 3 | source path uses 1 | PARTIALLY DONE |
| Menu rebuild P50/P95 | unavailable | slow rebuild logging added; no hardware sample | NOT VERIFIED |
| Idle Normal full audit | effectively about 5s due blocking timer | policy is 30s | PARTIALLY DONE |
| Idle Headless reconciliation | about 1s | policy is 10s | PARTIALLY DONE |
| Shell 740,000-byte stdout | previous implementation could deadlock | 209-543ms in two self-test runs, no timeout | PARTIALLY DONE |
| One-hour idle log growth | unavailable | rotation/no-op suppression implemented | NOT VERIFIED |

## Independent Audit

The first independent audit found R5-P1-01: configuration mutation interpreted a failed `ps` as no helper. A dedicated R5 PRD was created and implemented. The second audit found and fixed duplicate final Restore snapshots and missing Touch Bar Journal truth. The final static scans found no additional unremediated P0/P1 source defect, but XCTest and hardware evidence remain external blockers.

## Gate

R4: `PARTIALLY DONE`. v0.9.x Candidate Gate: `NOT VERIFIED`.
