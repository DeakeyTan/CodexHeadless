# V0.9 R3 Audit Remediation Report

## Scope

- Workspace commit: `a5c21ac38bf6e61a95063566e4c8a2a1d3200340` plus uncommitted R3 changes
- Workspace hash: `333ec392b5fba97b2ecf978b35763729c0e0781c6ee6c6efbf667c49c504df25`
- Version track: `v0.9.x`
- Mapping: `docs/V0.9_R3_Implementation_Mapping.md`

No R2 `DONE` result was accepted without source and command re-verification.

## P0

| ID | Status | Remediation and evidence |
|---|---|---|
| P0-01 | PARTIALLY DONE | Strategy A is wired into `EnableWorkflow`: unknown/unreliable original brightness refuses Enable before Journal, Keep Awake, virtual display, or handoff. Blind key events and fixed-value restoration were removed. Restore keeps replacement resources when verified restoration fails. `BrightnessSafetyTests` exists; XCTest executed 0, so DONE is prohibited. |
| P0-02 | PARTIALLY DONE | `KeepAwakeHost` directly owns an IOPM assertion; the recorded PID is the holder. Start requires assertion acknowledgement plus PID/start-time/file identity. Off is committed only after verified disappearance. `KeepAwakeHostLifecycleTests` exists; XCTest executed 0. |
| P0-03 | PARTIALLY DONE | Keep Awake and virtual display use intent/start/observe/Journal/RuntimeState commit stages. Failed cleanup retains the Journal. The Normal-mode compensation shortcut that could delete the sole credential was removed. Manager failure points are wired. Tests exist but did not execute. |
| P0-04 | PARTIALLY DONE | Recovery Journal performs a minimal schema probe first. Future schema is preserved and blocks read/update/delete/rebuild and process cleanup. Current damaged data is backed up. Compatibility tests exist but did not execute. |
| P0-05 | PARTIALLY DONE | `CleanNormalAssessor` checks RuntimeState, Journal, observed Keep Awake/virtual owners, managed display, built-in flags, Touch Bar, and a main physical display under workflow lock. Core, App, CLI, Doctor, and status consume the same assessment semantics. Tests did not execute. |

## P1

| ID | Status | Remediation and evidence |
|---|---|---|
| P1-01 | PARTIALLY DONE | Virtual reconcile and termination require canonical executable, file identity, start time, operation, instance, capability command fragments, and Journal ownership. PID-only trust was removed. XCTest executed 0. |
| P1-02 | PARTIALLY DONE | Normal mode no longer bypasses Journal/ownership checks. Unknown ownership is preserved and never terminated. XCTest executed 0. |
| P1-03 | PARTIALLY DONE | Internal helpers require an expiring one-time capability bound to helper kind, operation, parent PID, executable path, and file identity. Legacy underscore commands explicitly refuse execution. Authorization tests did not execute. |
| P1-04 | PARTIALLY DONE | App settings are disabled outside Clean Normal; `ConfigManager` also enforces a workflow lock and production mutation guard. XCTest executed 0. |
| P1-05 | PARTIALLY DONE | Cleanup progress records brightness restoration/verification, host stop, display disappearance, assertion-holder stop/disappearance, Touch Bar, RuntimeState, and Journal finalization. Completed stages are resumable across restart. Reentry tests did not execute. |

## P2

| ID | Status | Remediation and evidence |
|---|---|---|
| P2-01 | BLOCKED | `swift test --build-system native` exits 1 because the selected Command Line Tools installation has no `XCTest`. No full Xcode was found. Actual XCTest count: 0. |
| P2-02 | PARTIALLY DONE | All seven required test files exist and cover PID reuse, file identity, independent Journal/state failure, cleanup retention, and restart/reentry in source. Real helper/display lifecycle XCTest did not execute. |
| P2-03 | PARTIALLY DONE | All required `ManagedResourceFailurePoint` values are injected inside both managers. Test source covers the critical retained-Journal window, but XCTest did not execute. |
| P2-04 | PARTIALLY DONE | CLI application, support, internal helper, parser/model, output and exit-code concerns were split from the former monolith. `CLIExecutor.swift` remains 729 lines and the full suggested command-family split is not complete. |
| P2-05 | DONE | Fallback reads are classified in `docs/V0.9_R3_Fallback_Read_Audit.md`; `scripts/audit_critical_reads.sh` is enforced by CI and passed locally with exit 0. Critical workflows use throwing reads. |
| P2-06 | PARTIALLY DONE | Doctor/status show Clean Normal, Journal stage, recorded and observed owners, managed display facts, brightness status, expanded cleanup progress, and recommendation. XCTest and manual UI output verification were not executed. |
| P2-07 | DONE | README and Recovery Guide document Clean Normal, Journal/future schema, brightness restriction, assertion holder, resource drift, cleanupIncomplete, CLI result semantics, helper authorization, XCTest truth, and v0.9.x policy. |

## Safety Invariants

- Unknown original brightness causes pre-side-effect refusal.
- Unknown resource ownership never authorizes termination.
- Journal evidence is retained until resource disappearance is independently verified.
- Future Journal data is immutable to this build.
- `RuntimeState.mode == Normal` alone never authorizes Enable.
- A Journal finalization failure moves RuntimeState back out of Normal.

## Gate

`v0.9.x candidate gate`: **NOT VERIFIED**. P0/P1 tests and XCTest execution are blocked, and P2-04 remains incomplete.
