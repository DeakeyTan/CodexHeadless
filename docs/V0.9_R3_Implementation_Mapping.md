# CodexHeadless v0.9 R3 Implementation Mapping

This mapping was established before R3 code changes. R2 `DONE` labels are treated as claims to re-audit, not evidence.

## Baseline

- Branch/base: `main` / `a5c21ac`
- Workspace: dirty pre-existing R2 implementation; changes are preserved
- `pet-runs/`: untouched
- `swift build --build-system native`: exit 0
- `swift test --build-system native`: exit 1, XCTest methods executed: 0 (`no such module 'XCTest'`)
- Selected developer directory: `/Library/Developer/CommandLineTools`; no full Xcode found in `/Applications` or `/Library/Developer`

## P0 Mapping

| ID | Current code location | Current incorrect path | Safety invariant | Planned files | Planned tests | Acceptance |
|---|---|---|---|---|---|---|
| P0-01 | `BuiltInDisplayManager.swift:55-113`; `EnableWorkflow.swift:31-39,53,283-317`; `RestoreWorkflow.swift:229-244`; `RecoveryCoordinator.swift:21-41,143-145`; `TestDoubles.swift:88-105` | Production brightness read returns `nil`, but Accessibility capability permits blind dim; restore failure is recorded as skipped and cleanup continues; compensation restores `nil`; Fake defaults to 0.7. | Unknown original brightness must reject Enable before Journal, Keep Awake, virtual display, or display changes. If brightness was used, verified restoration precedes replacement/Keep Awake cleanup. | `BuiltInDisplayManager.swift`, `ManagerProtocols.swift`, `EnableWorkflow.swift`, `RestoreWorkflow.swift`, `RecoveryCoordinator.swift`, `Models.swift`, `TestDoubles.swift`, README/guide | `BrightnessSafetyTests.swift`; Enable/Restore regressions | `swift test --filter BrightnessSafetyTests`; full required command set |
| P0-02 | `SleepManager.swift:37-279`; `ManagedProcess.swift`; `Models.swift:581-657`; CLI helper dispatch | `/bin/sh` wrapper is recorded while child `/usr/bin/caffeinate` owns the assertion; wrapper death can orphan assertion; marker scan cannot prove child ownership; Normal bypass trusts state. | Recorded PID must be the assertion holder. Off only after identity and holder disappearance are observed. Unknown ownership is never killed. | `SleepManager.swift`, new `KeepAwakeHost.swift`, `ManagedProcess.swift`, `Models.swift`, `ManagerProtocols.swift`, CLI internal helper, `Paths.swift` | `KeepAwakeHostLifecycleTests.swift`; `SleepManagerTests.swift` | lifecycle filter; cross-process restore; `pmset -g assertions` manual checklist; full tests |
| P0-03 | `SleepManager.swift:183-244`; `VirtualDisplayManager.swift:80-220`; `RecoveryCoordinator.swift:21-84`; `RecoveryJournalStore.swift`; workflow-only failure injection | Resource starts before a durable intent; state failure triggers unverified terminate; outer compensation can see empty RuntimeState, delete Journal, and write Normal; delayed virtual display can appear after cleanup. | Reserve intent, start, observe, persist observed record, commit RuntimeState. Failed cleanup retains Journal and non-Normal recovery state. | `Models.swift`, `RecoveryJournalStore.swift`, `ManagedResourceCommit.swift`, `SleepManager.swift`, `VirtualDisplayManager.swift`, `RecoveryCoordinator.swift`, `RestoreWorkflow.swift`, failure injection | `ManagedResourceCommitTests.swift`; `RestoreReentryTests.swift` | manager-internal failure-window tests and full tests |
| P0-04 | `RecoveryJournalStore.swift:88-105`; `RestoreWorkflow.swift:416-443` | Future schema is fully decoded inside generic catch, gets a damaged backup, then Restore may delete and rebuild it from RuntimeState. | Future journal is read-only preserved: no backup, delete, overwrite, rebuild, process cleanup, or Normal write. | `RecoveryJournalStore.swift`, `Models.swift`, `RestoreWorkflow.swift`, `RecoveryCoordinator.swift`, Doctor/status | `RecoveryJournalCompatibilityTests.swift` | future-schema byte-for-byte preservation tests; full tests |
| P0-05 | `EnableWorkflow.swift:16-24`; `MenuActionAvailability.swift`; `CLIStateGate.swift`; `Doctor.swift`; `StatusReportBuilder.swift` | Enable checks only mode and Journal existence; residual state flags, verified/possible owners, managed display, soft disconnect, brightness, and Touch Bar are not assessed uniformly. | Normal is not Clean Normal. Enable re-assesses under workflow lock and starts no resource on any violation. Core/App/CLI/status/Doctor share one result. | new `CleanNormalAssessment.swift`, managers/protocols, `EnableWorkflow.swift`, controller, App/CLI gates, Doctor/status | `CleanNormalInvariantTests.swift` | matrix filters, self-test, full tests |

## P1 Mapping

| ID | Current code location | Current incorrect path | Safety invariant | Planned files | Planned tests | Acceptance |
|---|---|---|---|---|---|---|
| P1-01 | `VirtualDisplayManager.swift:223-251,342-350,429-451` | Reconcile checks only PID running and any managed display, then writes ID; PID reuse, ownership mismatch, Journal mismatch, or multiple hosts/displays can be adopted. | Reconcile mutates state only when PID, path, file identity, start time, instance, operation, Journal and display relation all match. | `VirtualDisplayManager.swift`, ownership observation models/protocols, Doctor/status | `ManagedResourceCommitTests.swift`, virtual lifecycle tests | ownership matrix filters |
| P1-02 | `SleepManager.swift:258-266`; `VirtualDisplayManager.swift:443-451` | `state.mode == .normal` makes RuntimeState its own trust root. Standalone managed resources have no independent sidecar. | State is never its own ownership authority; Journal or protected ownership sidecar is required in every mode. | new `ResourceOwnershipStore.swift`, `Paths.swift`, managers, models | Keep Awake/virtual sidecar tests, future-sidecar tests | ownership filters and full tests |
| P1-03 | `CLIExecutor.swift:50-115`; manager/private bridge invocations | Hidden helper names are directly callable and bypass lock, Journal stage, parent, operation and replay checks. | Every helper side effect consumes a one-time, expiring capability bound to helper kind, operation, parent and executable. | new `HelperCapabilityStore.swift`, CLI internal helper module, helper resolver, private bridges/managers, models/paths | `InternalHelperAuthorizationTests.swift` | no/wrong/expired/replay/parent/kind/valid matrix |
| P1-04 | `StatusBarSettingsActions.swift`; `StatusBarController.swift`; `ConfigManager.swift`; CLI config path | UI/action checks are incomplete and ConfigManager mutation has no Clean Normal/workflow guard; non-Normal changes remain possible outside one frontend. | Configuration mutation requires workflow lock plus Clean Normal at Core boundary; UI and action gates are defense in depth. | `ConfigManager.swift`, new mutation guard, App settings/menu, CLI config commands, composition roots | config mutation mode matrix in `CleanNormalInvariantTests.swift` | App/Core/CLI matrix filters |
| P1-05 | `Models.swift:53-83`; `RestoreWorkflow.swift:190-275,445-450` | Progress collapses host/display and holder/assertion stages; failed brightness can be `skipped`; state persisted and Journal finalization are ambiguous; errors often use `try?`. | Progress records actual physical, brightness, host, display, holder, assertion, Touch Bar, state and Journal stages; restart resumes unknown/pending without repeating verified side effects. | `Models.swift`, `RestoreWorkflow.swift`, `RecoveryCoordinator.swift`, managers, App/CLI presentation | `RestoreReentryTests.swift`; restore failure matrix | restart at every stage and full tests |

## P2 Mapping

| ID | Current code location | Current incorrect path | Safety invariant | Planned files | Planned tests | Acceptance |
|---|---|---|---|---|---|---|
| P2-01 | Test target and R2 report | 77 source methods were reported but zero XCTest executed; parser checks were not XCTest. | No unexecuted test is called PASS. | Package/CI as needed; R3 test report | all test groups | actual `swift test`, executed/pass/fail counts and environment recorded |
| P2-02 | Existing Fake-heavy test suite | Fakes model one PID and default brightness 0.7; no delayed resource/process-tree/commit-window lifecycle. | Test capabilities must match production; lifecycle observations are explicit. | seven mandatory R3 test files and test doubles | mandatory groups | actual XCTest execution |
| P2-03 | `WorkflowFailureInjection.swift` and workflow call sites | Injection occurs after manager returns and misses resource start/persistence/terminate windows. | Every manager transaction stage is injectable and preserves Journal on uncertain cleanup. | failure injection model, Sleep/Virtual managers, commit coordinator | `ManagedResourceCommitTests.swift` | all internal points executed |
| P2-04 | `CLIExecutor.swift` (770 lines) | Parsing/wiring partly split but command execution and internal helpers remain monolithic. | Dispatch is thin; internal helpers and mutation gates remain centralized and testable. | CLI status/workflow/config/display/experiment/repair/internal helper modules and formatter | parser/exit/state/helper tests | build, tests, CLI self-test |
| P2-05 | `stateStore.load`, `bestEffortUpdate`, `configManager.load` call sites | Read fallbacks are used in frontend and maintenance paths without a documented classification or CI guard. | Side-effect paths use lock + throwing reads + Core guard; fallback reads are display-only. | audit doc/table, critical call sites, CI static guard | read-boundary tests/static script | `rg` guard and full commands |
| P2-06 | `Doctor.swift`; `StatusReportBuilder.swift` | Reports mainly state flags and wrapper PID; no common Clean Normal, Journal compatibility, recorded/observed owner, assertion/display disappearance, or action. | Diagnostics distinguish recorded facts, observed facts, match/drift and recommended recovery. | Doctor/status models/builders and formatters | Doctor/status tests | self-test/status snapshots |
| P2-07 | `README.md`; `docs/RECOVERY_GUIDE.md`; missing R3 reports | R3 resource truth, future schema, helper authorization and actual verification status are absent. | Documentation describes implemented behavior and never upgrades unverified work. | README, Recovery Guide, four R3 reports | report validation/manual checklist | docs review, release verification |

## Required Validation Set

```bash
swift build --build-system native
swift test --build-system native
swift build --build-system native --arch arm64
swift build --build-system native --arch x86_64
bash -n scripts/*.sh
ruby -e 'require "yaml"; Dir[".github/workflows/*.yml"].each { |path| YAML.load_file(path) }'
git diff --check
.build/debug/codex-headless self-test
CODEX_HEADLESS_VERSION=0.9.2-test CODEX_HEADLESS_ARCH=universal bash scripts/build_release_assets.sh
CODEX_HEADLESS_VERSION=0.9.2-test CODEX_HEADLESS_ARCH=universal bash scripts/verify_release_assets.sh
```

XCTest must run under a full Xcode toolchain or a recorded full-Xcode CI run. Static parsing is not an acceptable substitute.
